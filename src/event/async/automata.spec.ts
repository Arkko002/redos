// createStateMachine(
//   ["A", "B", "C"],
//   [
//     { from: "A", to: "B" },
//     { from: "B", to: "C" },
//     { from: "C", to: "A" },
//   ],
// );
//
describe("StateMachine", () => {
  it("should allow for defining of states", () => {});
  it("should allow for defining of transitions between states", () => {});
  it("should allow for assigning exit and entry functions to states", () => {});
});

